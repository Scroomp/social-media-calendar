
  # Social Media Calendar (Copy)

  This is a code bundle for Social Media Calendar (Copy). The original project is available at https://www.figma.com/design/PHH0mv03QyaPpT7RaiT2br/Social-Media-Calendar--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # social-media-calendar
